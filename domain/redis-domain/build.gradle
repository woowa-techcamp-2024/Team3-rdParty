bootJar { enabled = false }
jar { enabled = true }

dependencies {
    api project(':core:test-container-core')

    api 'org.springframework.boot:spring-boot-starter-data-redis'
    api 'org.redisson:redisson-spring-boot-starter:3.17.6'
}