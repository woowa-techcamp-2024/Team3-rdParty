bootJar { enabled = true }
jar { enabled = true }

dependencies {
    implementation project (':core:exception-core')
    implementation project (':support:event-support')
    implementation project (':domain:jpa-domain')
    implementation project (':domain:redis-domain')
    implementation project(':api:api-support:security-support')

    testImplementation project(':api:api-support:rest-docs-support')
}