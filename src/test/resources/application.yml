spring:
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
  datasource:
    hikari:
      max-lifetime: 10

jwt:
  issuer: test
  expiry-seconds: 1800
  secret: thisisjusttestaccesssecretsodontworry

