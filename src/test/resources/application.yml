spring:
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    hikari:
      max-lifetime: 10

jwt:
  issuer: test
  expiry-seconds: 1800
  secret: thisisjusttestaccesssecretsodontworry

